apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kibana.fullname" . }}-config
  labels:
    {{- include "kibana.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.kibanaConfig }}
  {{ $key }}: |
{{ $value | indent 4 }}
  {{- end }}